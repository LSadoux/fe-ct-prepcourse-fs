function multiplica(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su multiplicación.
  // Tu código:

  return x * y;
}
var resultado = multiplica(6, 6);
console.log(resultado);


module.exports = multiplica;